{
  "id": "learningos-ui",
  "name": "LearningOS UI",
  "version": "0.4.0",
  "minAppVersion": "1.9.0",
  "description": "Desktop for LearningOS: dashboard home, Navigator rail, and a searchable Explorer over every note, concept, source, module and workspace - read straight from generated/manifest.json, so it works with no Python running. Capture to inbox; rebuild/validate via the los.py CLI. Presentation only - all rules live in the LearningOS core.",
  "author": "Aram + operator",
  "isDesktopOnly": true
}
